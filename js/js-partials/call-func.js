    /*=============================================
    = CALL FUNCTIONS
    =============================================*/
    var $o = slb.getObj();
    $(document).ready(slb.onReady.init);
    $(window).load(slb.onLoad.init)
    